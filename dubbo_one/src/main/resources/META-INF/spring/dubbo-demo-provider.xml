<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd ">
    <!-- 配置文件的地址 -->
    <context:property-placeholder location="config.properties" file-encoding="utf-8"/>

    <!-- 提供者应用名称，用来追踪依赖关系，调用了那个服务等等之类的 -->
    <dubbo:application name="demo-provider"/>

    <!-- zookeeper注册服务提供者消费者以及其他的一些配置 -->
    <dubbo:registry protocol="zookeeper" address="${dubbo.zookeeper.address}"/>

    <!-- dubbo被外部调用服务的协议及端口 20880 -->
    <dubbo:protocol name="dubbo" port="${dubbo.provider.port}"/>

    <!-- 服务实现 -->
    <bean id="sayHelloService" class="com.lwp.provider.SayHelloServiceImpl"/>

    <!-- 声明暴露的服务接口 -->
    <dubbo:service interface="com.lwp.api.SayHelloService" ref="sayHelloService" version="1.0.0">
        <dubbo:method name="sayHello" timeout="1000"/>
    </dubbo:service>
</beans>